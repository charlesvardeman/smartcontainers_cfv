# For more information about tox, see https://tox.readthedocs.org/en/latest/
[tox]
envlist = py27

[testenv]
deps = pytest
       pytest-pep8 
       pytest-cov
commands = py.test --ignore=build --pep8 -v --cov=MyPackage --cov-report=term-missing
